{
  "name": "notificationservice",
  "version": "1.0.0",
  "description": "This is the microservice that handles the interaction with FCM",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build && node ./lib",
    "serve": "node ./lib"
  },
  "author": "Sebastian Mora",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "firebase-admin": "^11.0.1"
  }
}
